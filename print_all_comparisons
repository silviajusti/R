print_interesting_data <- function(j, k) 
for (i in c(1:c(ncol(j)))){
  for(x in c(1:c(nrow(j)))){
    y<-(c(colnames(interesting_data)[i],row.names(interesting_data)[x],interesting_data[i,x]))
        write.table(y, file = k)
  }
}
